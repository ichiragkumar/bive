//! herdr-tui — Phase 2 stub.
//!
//! See `specs/11-phase-2-tui.md` for the full specification. The implementation
//! will consume `herdr-protocol` events over the daemon socket; nothing else is
//! required from the daemon.

fn main() {
    println!("herdr-tui: not implemented yet — see specs/11-phase-2-tui.md");
}
